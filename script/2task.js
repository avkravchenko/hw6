/* let a = 0;

function anyNumber(a){
    a = +a;

    if (Number.isInteger(a) && a <= 1000) {
        return `${a} is integer`;
    }

    else if (a > 1000) {
        return `The numeber is not correct`;
    }

    else return `${a} is not integer`;
}

console.log(anyNumber(a)); */